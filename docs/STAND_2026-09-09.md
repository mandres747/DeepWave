# Arbeitsstand 09.09.2026 — womit es weitergeht

Kurzfassung für den nächsten Arbeitstag. Ausführliche Begründungen stehen in
[`RHYTHMUS_LAYER_KONZEPT.md`](RHYTHMUS_LAYER_KONZEPT.md) und
[`../playstore/RELEASE_CHECKLIST.md`](../playstore/RELEASE_CHECKLIST.md).

---

## Wo die App gerade steht

| | |
|---|---|
| **Produktion** | **1.2.0 (versionCode 3)** — am 09.09. eingereicht, **wird von Google geprüft**. Verwaltete Veröffentlichung ist AUS, geht nach der Freigabe **automatisch live**. |
| Interner Test | 4 (1.2.1) — nur hochgeladen, um den Pre-Launch-Bericht auszulösen. Enthält den Rhythmus-Layer **nicht**. |
| Geschlossener Test (Alpha) | 2 (1.1.0), unverändert |
| Repo (`master`) | **1.3.0 / versionCode 5**, noch nicht gebaut, noch nicht hochgeladen |

Store-Einträge de-DE und en-US tragen die neuen 8 Marketing-Screenshots.
172 Länder, voller Roll-out.

---

## Sofort morgen prüfen

**Nachtrag 10.09.:** Der Track steht auf „Aktiv · Der Release 3 (1.2.0) wird
überprüft". Die Vorabprüfungen sind durch, die Sache liegt bei Google. Dass die
App-Liste weiter „Geschlossener Test" zeigt, ist korrekt — diese Spalte nennt
den höchsten Track, der **live** ist. Die drei Release-Empfehlungen wurden
abgearbeitet (siehe unten).

1. **Mail von Google** zur Freigabe von 1.2.0. Danach ist die App live.
2. **Pre-Launch-Bericht** unter *Testen und veröffentlichen → Pre-Launch-Bericht
   → Übersicht*. Wurde am 09.09. um 22:12 durch den internen Testrelease
   angestoßen. Falls dort **immer noch nichts** steht: Der frühere interne
   Release 1.0.0 vom 18.08. hat ebenfalls nie einen Bericht erzeugt — dann
   liegt es nicht an einer Einstellung (einen Aus-Schalter gibt es nicht) und
   die Frage an den Play-Support lohnt sich.

---

## Rhythmus-Layer: was fertig ist

Drei Commits: `a9308f3`, `91aa2be`, `a6f810a`.

- `audio/RhythmEngine.kt` — eigener AudioTrack, synthetisierter Woodblock-Klick
- `data/RhythmProgram.kt` — `RhythmPulse`, `RhythmPattern`, `RhythmStep`, `stepAt`
- `ui/screens/RhythmSheet.kt` — drei Modi **Tempo | Programm | Atem**, plus
  Kaufkarte im gesperrten Zustand
- `billing/Entitlements.kt` + zwei Implementierungen
- Programmablauf im `AudioPlaybackService` (überlebt Bildschirm-aus)

23 Unit-Tests grün. Im Emulator verifiziert: Wiedergabe, Moduswechsel im
laufenden Betrieb, Programmablauf mit Countdown, sauberes Beenden.

### Ausliefer-Schalter — nicht versehentlich umlegen

```
premium: rhythmLayerAvailable = true    (Feature im Build, Kauf entscheidet)
foss:    rhythmLayerAvailable = false   (Kauf-Feature, F-Droid kann nicht kassieren)
```

Zum Testen der entsperrten Oberfläche lokal `rhythmLayerAvailable = true` und
den `EntitlementsImpl`-Flow im FOSS-Flavour auf `true` patchen, Marker
`// TEMP-TEST` setzen, danach zurückdrehen und mit
`grep -rn "TEMP-TEST" app/src/` gegenprüfen.

---

## Offene Punkte

### Blockierend vor dem nächsten Release

1. **In-App-Produkt `rhythm_layer` in der Play Console anlegen**
   (Monetarisierung → Produkte → In-App-Produkte, einmalig/nicht verbrauchbar).
   Ohne das zeigt die App „Freischalten" ohne Preis und der Kauf schlägt fehl.
2. **Content-Rating-Fragebogen neu beantworten** — er fragt nach In-App-Käufen.
   Wird das übersprungen, blockiert es die Veröffentlichung. Antworten pflegen
   in `playstore/CONTENT_RATING.md`.
3. **Preis festlegen.** Die App kostet 3,99 €; 2–3 € für das Add-on wirken
   stimmig, ist aber eine Portfolio-Entscheidung.
4. **Store-Screenshots neu aufnehmen.** Die aktuellen zeigen die alte,
   einzeilige Werkzeugleiste; sie ist jetzt zweizeilig.
   Ablauf: `playstore/make_marketing_screenshots.py`, Rohaufnahmen nach
   `playstore/screenshots/raw/<locale>/`.

### Erledigt am 10.09.: die drei Release-Empfehlungen

Play zeigte zu Release 3 drei Aktionen. Zwei betrafen die randlose Anzeige und
trafen eigenen Code: `Theme.kt` setzte `statusBarColor`/`navigationBarColor`,
die Android 15 ignoriert und die auf älteren Versionen überschrieben, was
`enableEdgeToEdge()` gerade gesetzt hatte. Beide Zeilen raus, ebenso die
gleichen Farben aus `themes.xml`. Damit die Leisten jetzt wirklich transparent
sind, haben die drei Bottom-Sheets `navigationBars`-Insets und die zwei
Vollbild-Overlays sowie die Hauptliste `safeDrawing`-Insets bekommen.
Dritte Aktion: `isShrinkResources = true` im Release-Block ergänzt. Im
Release-Build gegengeprüft (Onboarding, Preset-Liste, Sheets) — keine fehlenden
Ressourcen, keine Abstürze.

**Nicht gemacht, bewusst:** Der von Play ebenfalls empfohlene Sprung auf
Android-Gradle-Plugin 9.0 (aktuell 8.11.1). Das ist ein Major-Update und dieses
Projekt hat mit `de.binauralbeats.app.foss` einen reproduzierbaren F-Droid-Build,
der dieselbe Toolchain braucht. Gehört in einen eigenen Durchgang mit
F-Droid-Gegenprobe.

### Feature-Reste aus v1 des Konzepts

5. **`SpokenCues`** — TTS-Ansagen bei Schrittwechseln („Tempo 120", „noch zwei
   Minuten"). Noch nicht angefangen.
6. **WAV-Export enthält die Puls-Spur nicht.** Wer exportiert, bekommt nicht,
   was er gehört hat. *Vorher aufräumen:* `WavExporter` dupliziert
   `applyModulation` aus `BinauralGenerator` schon heute — bevor ein dritter
   Aufrufer dazukommt, gehört die Funktion in eine gemeinsame Datei.

### Unabhängig davon

7. **F-Droid**: Tag `v1.2.0` und GitHub-Release mit dem FOSS-APK fehlen noch
   (WSL-Build, siehe Memory-Notiz). Unabhängig vom Play-Release.

---

## Nicht verifizierbar, bis Punkt 1 erledigt ist

Der eigentliche Kaufablauf. Der BillingClient verbindet sich und meldet zurück,
aber ohne angelegtes Produkt und ohne Build auf einem Track lässt sich weder
Kauf noch Wiederherstellung durchspielen. Auf dem Emulator meldet Play
zusätzlich „In-app billing API version 3 is not supported on this device" —
das ist eine Eigenheit des Emulators, kein Fehler im Code.

---

## Fallen, die diese Woche Zeit gekostet haben

- **OneDrive** sperrt `app/build`. Bei „Unable to delete directory" oder
  „not a regular file" den betroffenen Ordner löschen und neu bauen; half
  wiederholt bei `test-results`, `intermediates` und
  `merged_res_blame_folder`.
- **adb**: Zeitweise hängt neben dem Emulator ein echtes Gerät am Rechner.
  Immer `adb -s emulator-5554` verwenden, sonst landet die Installation auf
  dem Telefon.
- **Kotlin**: `var x by mutableStateOf(...)` mit `private set` erzeugt den
  JVM-Setter `setX()`. Eine eigene Funktion `fun setX(...)` kollidiert damit
  („platform declaration clash") — deshalb heißen die ViewModel-Funktionen
  `updateRhythm…`.
